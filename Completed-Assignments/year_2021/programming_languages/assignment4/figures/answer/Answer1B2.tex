\documentclass{standalone}
\usepackage{tikz} % Import the tikz package
\usetikzlibrary{automata} % Import library for drawing automata
\usetikzlibrary{positioning} % ...positioning nodes
\usetikzlibrary{arrows} % ...customizing arrows
\tikzset{node distance=2.5cm,
    every state/.style={
        semithick,
        fill=gray!10},
    initial text={},
    double distance=2pt,
    every edge/.style={
        draw,
        ->,>=stealth',
        auto,
        semithick}}
\let\epsilon\varepsilon
\begin{document}
    \begin{tikzpicture}
        \node[state,initial] (start) {$-$};
        \node[state,right of=start] (ul) {$1$};
        %\node[state, below of=ul] (bl) {$2$};
        \node[state,right of=ul] (um) {$3$};
        \node[state, right of=um] (ur) {$4$};
        \node[state, below of=ur] (br) {$5$};
        \node[state, right of=br] (end) {$+$};
        
        \draw (start) edge[] node {$\Lambda$} (ul);
        \draw (br) edge[] node {$\Lambda$} (end);
        
        \draw (ul) edge[loop above] node {\tt a} (ul);
        %\draw (ul) edge[left] node {\tt bb} (bl);
        \draw (ul) edge[] node {\tt ab} (um);
        \draw (ul) edge[] node {\tt bb + abb} (br);

        \draw (um) edge[] node {\tt ba} (ur);
        
        %\draw (bl) edge[] node {$\Lambda$} (br);

        \draw (ur) edge[] node {$\Lambda$} (br);

        \draw (br) edge[loop below] node {\tt a,b} (br);
    \end{tikzpicture}
\end{document}